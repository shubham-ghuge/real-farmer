// import React, { createContext, useContext } from "react";
// import { QuizData } from "../data/quizData.types";
// import { quizData } from "../data/quizData";

// const QuizContext = createContext<QuizData | null>(null);

// export const QuizContextProvider = ({ children }: React.ReactChild) => {
//   return (
//     <QuizContext.Provider value={quizData}>{children}</QuizContext.Provider>
//   );
// };

// export default function useQuizContext() {
//   return useContext(QuizContext);
// }
export {};
